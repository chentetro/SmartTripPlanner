<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.SmartTripPlanner" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/bg_cream</item>
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="colorSecondary">@color/secondary_coral</item>
        <item name="android:statusBarColor">@color/bg_cream</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.SmartTripPlanner" parent="Base.Theme.SmartTripPlanner" />

    <!-- Style for rounded images in RecyclerView Items -->
    <style name="RoundedImageView" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">20dp</item>
    </style>
</resources>